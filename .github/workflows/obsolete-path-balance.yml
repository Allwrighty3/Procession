name: Obsolete Path Balance Diagnostic

on:
  pull_request:
    paths:
      - ".github/workflows/obsolete-path-balance.yml"
      - "lib/procession/simulation/obsolete_path_balance_experiment.ex"
      - "lib/procession/simulation/action_cost_reversal_experiment.ex"
      - "lib/mix/tasks/procession.metrics.obsolete_path_balance.ex"
      - "lib/mix/tasks/procession.metrics.action_cost_reversal.ex"
      - "test/procession/simulation/obsolete_path_balance_experiment_test.exs"
      - "test/procession/simulation/action_cost_reversal_experiment_test.exs"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      MIX_ENV: test
      OUTPUT: tmp/action-cost-reversal/rows.jsonl
      SUMMARY: tmp/action-cost-reversal/summary.txt

    steps:
      - uses: actions/checkout@v4

      - uses: erlef/setup-beam@v1
        with:
          otp-version: "25"
          elixir-version: "1.14"

      - name: Install dependencies
        run: mix deps.get

      - name: Compile with warnings as errors
        run: mix compile --warnings-as-errors

      - name: Run focused tests
        run: |
          mix test test/procession/simulation/obsolete_path_balance_experiment_test.exs
          mix test test/procession/simulation/action_cost_reversal_experiment_test.exs

      - name: Run full suite
        run: mix test

      - name: Run paired 100-seed action-cost diagnostic
        run: |
          mix procession.metrics.action_cost_reversal \
            --seeds 100 \
            --output "$OUTPUT" \
            --summary-output "$SUMMARY"
          echo "## Action Cost Reversal Results" >> "$GITHUB_STEP_SUMMARY"
          echo '```text' >> "$GITHUB_STEP_SUMMARY"
          cat "$SUMMARY" | tee -a "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Verify output
        run: |
          test "$(wc -l < "$OUTPUT")" -eq 200
          grep -q '^control:' "$SUMMARY"
          grep -q '^action_cost:' "$SUMMARY"

      - uses: actions/upload-artifact@v4
        with:
          name: action-cost-reversal-results
          path: |
            ${{ env.OUTPUT }}
            ${{ env.SUMMARY }}
          if-no-files-found: error
          retention-days: 30
