name: Cascade Compile Diagnostic

on:
  pull_request:
    branches: [main]

jobs:
  diagnose:
    if: github.head_ref == 'agent/cascading-regional-change'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: erlef/setup-beam@v1
        with:
          otp-version: '27'
          elixir-version: '1.18.3'
      - run: mix deps.get
      - name: Capture compile
        shell: bash
        run: |
          set +e
          mix compile --warnings-as-errors > cascade-compile.txt 2>&1
          cat cascade-compile.txt
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: cascade-compile
          path: cascade-compile.txt
